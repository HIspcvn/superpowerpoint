# Third-party notices

The optional demo builder calls `@oai/artifact-tool` supplied by a compatible host. That library and its fonts are not distributed as source dependencies or binaries in this repository. Their own licensing and availability apply.
